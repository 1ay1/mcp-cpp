Vendored third-party sources. Both MIT (see each LICENSE).

tree-sitter/        runtime,  github.com/tree-sitter/tree-sitter       v0.25.10
                    lib/src + lib/include copied verbatim; src/wasm/ dropped
                    (only built with TREE_SITTER_FEATURE_WASM, which we never set).
tree-sitter-bash/   grammar,  github.com/tree-sitter/tree-sitter-bash  a06c2e4 (0.25.1)
                    src/parser.c, src/scanner.c, src/tree_sitter/ copied verbatim.

Used by src/tools/util/shellx.cpp to parse agent shell commands into an AST.
To update: replace these files from the upstream tags above, rebuild, and run
the shellx tests + the real-command corpus probe. Do not edit them in place.
